---
# file: roles/ansible-common/vars/Ubuntu-14.yml

#
# variables for Ubuntu 14.04.x LTS (Trusty Tahr)
#

# essential packages for ansible modules
__essential_packages:
  - python-apt
  - python-selinux
  - python-semanage
  - python-lxml
  - git
  - unzip

# common packages for every Ubuntu 14 system
__common_packages:
  - net-tools
  - psmisc
  - curl
