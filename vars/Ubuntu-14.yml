---
# file: roles/ansible-common/vars/Ubuntu-14.yml

#
# variables for Ubuntu 14.04.x LTS (Trusty Tahr)
#

# list of essential packages for ansible modules
__essential_packages:
  - python-apt
  - python-selinux
  - python-semanage
  - python-lxml
  - git
  - unzip

# list of common packages
__common_packages:
  - net-tools
  - psmisc
  - curl
