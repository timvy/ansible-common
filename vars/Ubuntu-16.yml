---
# file: roles/ansible-common/vars/Ubuntu-16.yml

#
# variables for Ubuntu 16.04.x LTS (Xenial Xerus)
#

# essential packages for ansible modules
__essential_packages:
  - python-apt
  - python-selinux
  - python-semanage
  - python-lxml
  - git
  - unzip

# common packages for every Ubuntu 16 system
__common_packages:
  - net-tools
  - psmisc
  - curl
