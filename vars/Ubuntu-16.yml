---
# file: roles/ansible-common/vars/Ubuntu-16.yml

#
# variables for Ubuntu 16.04.x LTS (Xenial Xerus)
#

# list of essential packages for ansible modules
__essential_packages:
  - python-apt
  - python-selinux
  - python-semanage
  - python-lxml
  - git
  - unzip

# list of common packages
__common_packages:
  - net-tools
  - psmisc
  - curl
