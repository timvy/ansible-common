---
# file: roles/ansible-common/meta/main.yml

galaxy_info:
  author: Cogline.v3
  description: Setup defaults (Ansible module dependencies, software packages, bash dotfiles, etc.) for every supported Linux distribution.
  license: BSD
  min_ansible_version: 2.7
  #github_branch:
  platforms:
    - name: EL
      versions:
      - 7
      - 6
    - name: Ubuntu
      versions:
      - artful
      - trusty
      - xenial
      - cosmic
      - bionic
    - name: Debian
      versions:
      - jessie
      - stretch
    
  galaxy_tags:
    - ansible
    - common
    - packages
    - setup
    - installation

dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
