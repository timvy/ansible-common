---
# file: roles/ansible-common/meta/main.yml

galaxy_info:
  author: Cogline.v3
  description: Set up dependencies for Ansible standard modules and install default software packages for each supported Linux distribution.
  license: BSD
  min_ansible_version: 2.7
  #github_branch:
  platforms:
    - name: Alpine
      versions:
      - any
    - name: EL
      versions:
      - 7
      - 6
    - name: Ubuntu
      versions:
      - artful
      - trusty
      - xenial
      - cosmic
      - bionic
    - name: Debian
      versions:
      - jessie
      - stretch
    
  galaxy_tags:
    - ansible
    - common
    - packages
    - setup
    - installation

dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
